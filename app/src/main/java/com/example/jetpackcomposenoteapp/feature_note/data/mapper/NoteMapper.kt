package com.example.jetpackcomposenoteapp.feature_note.data.mapper


/*

fun Note.toEntity()=NoteEntity(
    title, timestamp,isComplete, id
)
fun NoteEntity.toNote()= Note(
    title, timestamp,isComplete ,id
)*/
